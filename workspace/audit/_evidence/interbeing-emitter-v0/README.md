# Interbeing Emitter Evidence

This directory stores local-only evidence emitted by `npm run interbeing:emit`.

- Runtime JSON evidence stays untracked.
- The canonical task envelope is written directly into Dali's intake path.
- Adapter-local fields such as `child_task_id` stay here instead of crossing the Interbeing v0 boundary.
